<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.mapper.ReplyMapper">

	<!-- 댓글조회 -->
	<select id="replyList" parameterType="int" resultType="reply">
		select b.*
		from   (select /*INDEX_DESC(r PK_REPLY) */ r.*, rownum rn
		        from tbl_reply r
		        where r.board_no = #{boardNo}) b
		where b.rn > (#{page} - 1) * 5
		<![CDATA[
	    and b.rn <= (#{page} * 5)
		]]>	      
	</select>
	
	<!-- 댓글삭제 -->
	<delete id="deleteReply" parameterType="int">
		delete from tbl_reply
		where reply_no = #{replyNo}
	</delete>
	
	<!-- 댓글등록 -->
	<insert id="insertReply" parameterType="reply">
		<selectKey keyProperty="replyNo" resultType="int" order="BEFORE">
			select reply_seq.nextVal from dual
		</selectKey>
	
		insert into tbl_reply (reply_no, board_no, reply, replyer)
		values (#{replyNo}, #{boardNo}, #{reply}, #{replyer})
	</insert>
	
	<!-- 댓글건수 계산 -->
	<select id="selectCount" parameterType="int">		
		select count(1)
		from tbl_reply
		where board_no = #{boardNo}
		group by board_no
	</select>
	
	
	<!-- Event 관련 기능 -->
	<!-- 이벤트 목록 조회 -->
	<select id="eventList" resultType="com.yedam.vo.EventVO">
		select title      as "title",
		       start_date as "start",
		       end_date   as "end"
		from tbl_event
	</select>
	
	<!-- 이벤트 등록 -->
	<insert id="insertEvent" parameterType="com.yedam.vo.EventVO">
		insert into tbl_event (title, start_date, end_date)
		values (#{title}, #{start}, #{end})
	</insert>
	
	<!-- 이벤트 삭제 -->
	<delete id="deleteEvent" parameterType="String">
		delete from tbl_event
	    where title = #{title}
	</delete>
	<!-- parameterType="String"으로 들어오면 mybatis에서 값을 #{value}이렇게 받아야지 동작함
	아니면 Mapper인터페이스에서 @Param("title") String title이렇게 매개변수 전달해야함  -->
	
	

</mapper>